{{ author }}
from django.db import models
from django.utils.translation import gettext_lazy as _

class {{camel_case_model_name}}(models.Model):
    # fields

    class Meta:
        # ordering = ['field',]
        verbose_name = _('{{ camel_case_model_name }}')
        verbose_name_plural = _('{{ camel_case_model_name }}s')

    def __str__(self):
        pass
        # return f' {}'